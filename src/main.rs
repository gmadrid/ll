//use loveletter::{GameBuilder};

fn main() -> std::result::Result<(), loveletter::LoveLetterError> {
    // let game = GameBuilder::default()
    //     .num_players(4)
    //     .build()?;

    // println!("GAME: {}", game);
    Ok(())
}
